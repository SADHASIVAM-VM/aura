[may-10]

TO generate a secure JWT secrect key by using the 
{" node -e "console.log(require('crypto').randomBytes(64).toString('hex'))" In CLI }

##LOGIN
singin and login was completed by using supabase as db and JWT for authentication